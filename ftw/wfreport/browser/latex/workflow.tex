\section*{${title}}

\includegraphics[width=\textwidth]{${graph}}

% if footnotes:
\begin{enumerate}
  % for text in footnotes:
    \item ${text}
  % endfor
% endif

\clearpage

% for state in data.get_states():
\subsection*{${state.title}}

\begin{longtable}{|l|${'c' * len(data.get_roles())}|}
  \hline
  {\bf ${convert(_('Permission'))}}
  % for role in data.get_roles():
  & \begin{sideways}{\bf ${convert(role.title)}}\phantom{x}\end{sideways}
  % endfor
  \hline
  \endhead

  % for permission in state.permission_mapping:
    ${convert(permission.title)}
    % if permission.get('merged'):
      \footnote{{\bf ${convert(permission.title)}}: ${translate_permissions(permission.merged)}}
    % endif
    % for role in data.get_roles():
      & ${role in permission.roles and '\ensuremath{\\surd}' or ''}
    % endfor
    \\ %%
    \hline
  % endfor
\end{longtable}

\clearpage
\setcounter{footnote}{0}
% endfor
